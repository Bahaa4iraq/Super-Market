import 'package:flutter/material.dart';

class OutlyPage extends StatelessWidget {
  const OutlyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Outly"),
    );
  }
}
